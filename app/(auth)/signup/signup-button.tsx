"use client";

export default function SignUpButton() {
  return (
    <button
      className="btn mt-6 w-full bg-linear-to-t from-blue-600 to-blue-500 bg-[length:100%_100%] bg-[bottom] text-white shadow-sm hover:bg-[length:100%_150%]"
      onClick={async () => {
        // 
      }}
    >
      Register
    </button>
  );
}
